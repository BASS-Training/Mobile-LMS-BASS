import 'package:flutter/material.dart';

/// Shared colour palette for the onboarding illustrations.
///
/// Tuned to read vividly on the red brand gradient panel: warm cream surfaces,
/// peach skin, teal apparel, coral highlights, and golden accents. Keeping it in
/// one place makes the three intro illustrations feel like one set.
class IntroArt {
  const IntroArt._();

  static const cream = Color(0xFFFFF6E9);
  static const creamDeep = Color(0xFFE9D6B6);
  static const paper = Color(0xFFFFFDF8);

  static const skin = Color(0xFFFFC79F);
  static const skinDeep = Color(0xFFEFA679);

  static const hair = Color(0xFF332F52);
  static const ink = Color(0xFF433E63);

  static const teal = Color(0xFF24C9A4);
  static const tealDeep = Color(0xFF12A98A);

  static const gold = Color(0xFFFFD24C);
  static const goldDeep = Color(0xFFF3B330);

  static const coral = Color(0xFFFF7A6B);
  static const coralDeep = Color(0xFFEC5A53);

  static const sky = Color(0xFF5BB8FF);
}
